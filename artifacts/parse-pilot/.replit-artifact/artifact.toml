kind = "web"
previewPath = "/"
title = "ParsePilot AI"
version = "1.0.0"
id = "artifacts/parse-pilot"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24127

[services.development]
run = "pnpm --filter @workspace/parse-pilot run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/parse-pilot", "run", "build" ]
publicDir = "artifacts/parse-pilot/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24127"
BASE_PATH = "/"
